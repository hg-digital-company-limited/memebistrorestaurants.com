<!-- <script>
    window.addEventListener('scroll', function() {
        const section = document.querySelector('.h-section2');
		 const fixxx = document.querySelector('#xfixxx');
        if (window.scrollY > 1000) {

            section.style.position = 'fixed'; // Vị trí cố định
            section.style.top = '0'; // Ở trên cùng
            section.style.zIndex = '999'; // Độ ưu tiên hiển thị
			  fixxx.style.opacity = '0';
        } else if (window.scrollY < 800) {
         section.style.position = 'initial'; // Vị trí cố định
            section.style.top = 'initial'; // Ở trên cùng
            section.style.zIndex = 'initial'; // Độ ưu tiên hiển thị
			  fixxx.style.opacity = '1';

        }
    });
</script> -->